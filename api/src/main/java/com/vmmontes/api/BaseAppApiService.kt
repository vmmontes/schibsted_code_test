package com.vmmontes.api

import retrofit2.Call
import retrofit2.http.GET
import retrofit2.http.Path

interface BaseAppApiService {

    /*@GET("history?start_at={start_date}&end_at={end_date}&base=GBP&symbols=EUR")
    fun getHistory(@Path("start_date") startDate : String,
              @Path("end_date") endDate : String) : Call<BaseResponseApiModel>*/
}